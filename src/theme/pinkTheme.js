import { createTheme } from "@mui/material";

export const pinkTheme = createTheme({
  palette: {
    primary: {
      main: "#FF9494"
    },

    secondary: {
      main: "#FFD1D1"
    },

    warnin: {
      main: "#FFE3E1"
    },

    error: {
      main: "#FFF5E4"
    },
  },
});
